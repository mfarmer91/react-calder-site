import React, { Component } from 'react';

const ImgContainer = () => (
    <div id='img_container' className='container'>this is a work around of routing with img.</div>
);

export default ImgContainer;